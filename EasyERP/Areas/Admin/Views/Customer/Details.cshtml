@using EasyERP.Helpers
@model EasyERP.Models.Customer

@{
    ViewBag.Title = "Klient - Szczegóły";
}

<div class="actions">
    <ul>
        <li>@Html.ActionLink("edycja", "Edit", "Customer", new { id=Model.Id }, null)</li>
        <li>@Html.ActionLink("odśwież", "Details")</li>
    </ul>
</div>

<table class="display-record">
    <tbody>
        <tr>
            <td>Nazwa</td>
            <td>@Html.DisplayFor(model => model.Name)</td>
        </tr>
        <tr>
            <td>Kod pocztowy</td>
            <td>@Html.DisplayFor(model => model.ZipCode)</td>
        </tr>
        <tr>
            <td>Miasto</td>
            <td>@Html.DisplayFor(model => model.City)</td>
        </tr>
        <tr>
            <td>Ulica</td>
            <td>@Html.DisplayFor(model => model.Street)</td>
        </tr>
        <tr>
            <td>Telefon</td>
            <td>@Html.DisplayFor(model => model.Telephone)</td>
        </tr>
        <tr>
            <td>Email</td>
            <td>@Html.DisplayFor(model => model.Email)</td>
        </tr>
    </tbody>
</table>

<table class="display-records">
    <thead>
        <tr>
            <th>Id</th>
            <th>Typ</th>
            <th>Nazwa</th>
            <th>Cena</th>
            <th></th>
        </tr>
    </thead>

    <tbody>

@{int i = 0;}
@foreach (var order in Model.Orders) {
        <tr class="row@(i % 2)">
            <td>@Html.DisplayFor(modelItem => order.Id)</td>
            <td>@ProductHelpers.DisplayTypeName(order.ProductType)</td>
            <td>@Html.DisplayFor(modelItem => order.ProductName)</td>
            <td>@PriceFormatHelper.DisplayFormatPrice(order.ProductPrice)</td>
            <td>
                @Html.ActionLink("szczegóły", "Details", "Order", new { id = order.Id }, null)
            </td>
        </tr>
    i = i + 1;
}
    </tbody>
</table>
